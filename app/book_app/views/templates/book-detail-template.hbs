<a class="close" data-dismiss="modal">x</a>
    <div class="imgBook">
      <img src="{{#if thumbnail}}
                  {{thumbnail}}
                {{else}}
                  http://placehold.it/100x150
                {{/if}}"
       />
    </div>
    <h1>{{ title }}</h1>

     {{#if subtitle}} <h2>{{ subtitle }}</h2> {{/if}}
     {{#if description}} {{description}} {{else}}No description found{{/if}}

    <b>Google link :</b> <a href="http://books.google.fr/books?id={{ googleId }}" target="_blank">http://books.google.fr/books?id={{ googleId }}</a>
